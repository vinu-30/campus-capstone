// Shows existing teams with dummy administrative actions.
import {useState} from 'react';import DashboardLayout from '../../layouts/DashboardLayout';import TeamCard from '../../components/admin/TeamCard';const teams=[{name:'Team Innovators',project:'Campus Matchmaker',leader:'Priya Sharma',members:4,status:'Active'},{name:'Green Minds',project:'EcoTrack Analytics',leader:'Maya Patel',members:3,status:'Active'}];function TeamManagement(){const[note,setNote]=useState('');return <DashboardLayout><main className="admin-page"><div className="admin-heading"><h1>Team Management</h1><p>Monitor created teams and their project status.</p></div>{note&&<div className="alert alert-warning">{note} team.</div>}<div className="admin-grid">{teams.map(x=><TeamCard key={x.name} team={x} onAction={(a,n)=>setNote(`${a} ${n}`)}/>)}</div></main></DashboardLayout>};export default TeamManagement;

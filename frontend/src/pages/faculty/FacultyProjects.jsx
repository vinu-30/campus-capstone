// Project approval page with dummy review cards and feedback alerts.
import {useState} from 'react';import DashboardLayout from '../../layouts/DashboardLayout';import ProjectReviewCard from '../../components/faculty/ProjectReviewCard';
const projects=[{title:'Smart Campus Navigator',lead:'Alex Morgan',members:'4 students',description:'A campus discovery application for rooms and services.',tech:['React','Maps API','Figma'],status:'Pending Review'},{title:'EcoTrack Analytics',lead:'Maya Patel',members:'3 students',description:'Analytics dashboard for university sustainability data.',tech:['Python','MySQL','Charts'],status:'Pending Review'}];
function FacultyProjects(){const[note,setNote]=useState('');return <DashboardLayout><main className="faculty-page"><div className="faculty-heading"><h1>Project Reviews</h1><p>Approve proposals or guide students with changes.</p></div>{note&&<div className="alert alert-info">Project {note}.</div>}<div className="faculty-project-grid">{projects.map(p=><ProjectReviewCard key={p.title} project={p} onAction={(action,title)=>setNote(`${title} was ${action}`)}/>)}</div></main></DashboardLayout>};export default FacultyProjects;

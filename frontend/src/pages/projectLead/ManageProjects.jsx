// Lists locally stored dummy projects and supports visual deletion.
import {useState} from 'react';import DashboardLayout from '../../layouts/DashboardLayout';import ProjectCard from '../../components/projectLead/ProjectCard';
const data=[{title:'Smart Campus Navigator',description:'Campus navigation and service discovery mobile experience.',skills:['React','UI/UX','API'],members:4,status:'Active'},{title:'EcoTrack Analytics',description:'Data insights for sustainable campus operations.',skills:['Python','MySQL','Charts'],members:3,status:'In Review'},{title:'MediConnect Platform',description:'Healthcare booking platform for university students.',skills:['Node.js','React','Figma'],members:4,status:'Planning'}];
function ManageProjects(){const[projects,setProjects]=useState(data);return <DashboardLayout><main className="lead-page"><div className="lead-heading"><div><h1>My Projects</h1><p>View, update, and organize your capstone projects.</p></div></div><div className="lead-project-grid">{projects.map((project)=><ProjectCard key={project.title} project={project} onDelete={(title)=>setProjects(projects.filter((item)=>item.title!==title))}/>)}</div></main></DashboardLayout>};export default ManageProjects;

// Local-only form for creating a dummy project; no backend data is sent.
import {useState} from 'react';import {useNavigate} from 'react-router-dom';import DashboardLayout from '../../layouts/DashboardLayout';
function CreateProject(){const nav=useNavigate(),[saved,setSaved]=useState(false);const submit=(e)=>{e.preventDefault();setSaved(true)};const fields=[['Project Title','text'],['Project Category','text'],['Required Technologies','text'],['Required Skills','text'],['Number of Team Members','number'],['Project Duration','text'],['Project Deadline','date']];return <DashboardLayout><main className="lead-page"><div className="lead-heading"><div><h1>Create Project</h1><p>Define the requirements for a new capstone project.</p></div></div>{saved&&<div className="alert alert-success">Project created locally in demo mode.</div>}<form className="lead-panel create-form" onSubmit={submit}><div className="row g-3">{fields.map(([label,type])=><div className="col-md-6" key={label}><label className="form-label">{label}</label><input className="form-control" type={type} required/></div>)}<div className="col-12"><label className="form-label">Project Description</label><textarea className="form-control" rows="4" required/></div></div><div className="mt-4 d-flex gap-2"><button className="btn btn-primary">Create Project</button><button type="button" className="btn btn-outline-secondary" onClick={()=>nav('/lead/dashboard')}>Cancel</button></div></form></main></DashboardLayout>};export default CreateProject;

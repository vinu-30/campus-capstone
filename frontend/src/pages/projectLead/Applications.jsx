// Shows dummy student applications with accept and reject feedback.
import {useState} from 'react';import DashboardLayout from '../../layouts/DashboardLayout';import ApplicationCard from '../../components/projectLead/ApplicationCard';
import applicationService from '../../services/applicationService';
function Applications(){const[applications,setApplications]=useState(()=>applicationService.getApplications());const[notice,setNotice]=useState('');const handleAction=(id,status)=>{const application=applicationService.updateStatus(id,status);setApplications(applicationService.getApplications());setNotice(`${application.studentName}'s ${application.projectTitle} application is ${status}.`)};return <DashboardLayout><main className="lead-page"><div className="lead-heading"><div><h1>Student Applications</h1><p>Review applicants for your active projects.</p></div></div>{notice&&<div className="alert alert-info">{notice}</div>}<section className="lead-panel table-responsive"><table className="table lead-table"><thead><tr><th>STUDENT</th><th>DEPARTMENT</th><th>PROJECT & SKILLS</th><th>EXPERIENCE</th><th>APPLIED</th><th>STATUS</th><th>ACTIONS</th></tr></thead><tbody>{applications.map((item)=><ApplicationCard key={item.id} application={item} onAction={handleAction}/>)}</tbody></table></section></main></DashboardLayout>};export default Applications;

// Full-screen split Login page with role selection on the dark project panel.
import { useState } from 'react';
import { Link } from 'react-router-dom';
import LoginForm from '../components/LoginForm';
import RoleCard from '../components/RoleCard';

const roles=[{role:'Student',icon:'bi-mortarboard-fill',description:'Build your skills and join a team.'},{role:'Project Lead',icon:'bi-diagram-3-fill',description:'Manage projects and team members.'},{role:'Faculty Advisor',icon:'bi-person-workspace',description:'Review and guide student teams.'},{role:'Admin',icon:'bi-shield-check',description:'Manage the complete platform.'}];
function Login(){const[selectedRole,setSelectedRole]=useState('Student');return <main className="split-login"><section className="login-showcase"><div className="showcase-content"><div className="showcase-logo"><i className="bi bi-mortarboard-fill"/></div><p className="showcase-label">CAMPUS CAPSTONE</p><h1>Project Team Matchmaker <span>&amp; Skill-Gap Auditor</span></h1><p className="showcase-text">Find the right teammates, build stronger projects, and grow your skills together.</p><div className="showcase-roles"><div className="showcase-role-title"><strong>Select your role</strong><span>Choose how you want to continue</span></div><div className="role-grid">{roles.map(item=><RoleCard key={item.role} {...item} selected={selectedRole===item.role} onSelect={setSelectedRole}/>)}</div></div></div><small className="showcase-footer">© 2026 Campus Capstone Platform</small></section><section className="login-form-side"><div className="login-form-wrap"><div className="mobile-brand"><i className="bi bi-mortarboard-fill"/> Campus Capstone</div><h2>Welcome back</h2><p>Sign in to access your {selectedRole} workspace.</p><LoginForm selectedRole={selectedRole}/><p className="register-prompt">New to Campus Capstone? <Link to="/register">Create an account</Link></p></div></section></main>};export default Login;

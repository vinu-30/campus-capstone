// Creates consistent asynchronous CRUD services using local in-memory dummy data.
export default function createMockService(seedData) { let records = [...seedData]; const respond = (value) => new Promise((resolve) => setTimeout(() => resolve(value), 700)); return { getAll: async () => respond([...records]), getById: async (id) => respond(records.find((item) => String(item.id) === String(id)) || null), create: async (data) => { const item = { id: Date.now(), ...data }; records = [...records, item]; return respond(item); }, update: async (id, data) => { records = records.map((item) => String(item.id) === String(id) ? { ...item, ...data } : item); return respond(records.find((item) => String(item.id) === String(id)) || null); }, delete: async (id) => { records = records.filter((item) => String(item.id) !== String(id)); return respond(true); } }; }

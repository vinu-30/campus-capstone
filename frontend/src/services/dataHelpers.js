// Reusable client-side search, filtering, sorting, and pagination helper.
export function queryRecords(records,{search='',filter='',sort='name',page=1,pageSize=5}){const text=search.toLowerCase();let result=records.filter(x=>JSON.stringify(x).toLowerCase().includes(text));if(filter)result=result.filter(x=>Object.values(x).some(v=>String(v).toLowerCase()===filter.toLowerCase()));result=[...result].sort((a,b)=>String(a[sort]||a.name||'').localeCompare(String(b[sort]||b.name||'')));return {items:result.slice((page-1)*pageSize,page*pageSize),totalPages:Math.max(1,Math.ceil(result.length/pageSize))};}

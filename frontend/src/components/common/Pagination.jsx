// Reusable Bootstrap pagination navigation.
function Pagination({page=1,totalPages=1,onChange}){return <nav><ul className="pagination justify-content-end mb-0"><li className={`page-item ${page===1?'disabled':''}`}><button className="page-link" onClick={()=>onChange(page-1)}>Previous</button></li>{Array.from({length:totalPages},(_,i)=><li className={`page-item ${page===i+1?'active':''}`} key={i}><button className="page-link" onClick={()=>onChange(i+1)}>{i+1}</button></li>)}<li className={`page-item ${page===totalPages?'disabled':''}`}><button className="page-link" onClick={()=>onChange(page+1)}>Next</button></li></ul></nav>}export default Pagination;

// Shared top navbar showing the active local demo user.
import { getCurrentUser } from '../services/authService';
function TopNavbar({onMenuClick}){const user=getCurrentUser();const initials=(user?.email||'CC').slice(0,2).toUpperCase();return <header className="top-navbar"><button className="btn menu-button d-lg-none" onClick={onMenuClick}><i className="bi bi-list"/></button><div className="page-heading"><h1>Campus Capstone</h1><p>Project Team Matchmaker &amp; Skill-Gap Auditor</p></div><div className="navbar-actions"><button className="icon-button position-relative"><i className="bi bi-bell"/><span className="notification-dot"/></button><div className="profile-info"><div className="profile-avatar">{initials}</div><span className="d-none d-sm-block"><strong>{user?.email?.split('@')[0]||'User'}</strong><small>{user?.role||'User'}</small></span></div></div></header>};export default TopNavbar;

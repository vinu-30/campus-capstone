// Reusable card for reviewing a submitted student project.
function ProjectReviewCard({project,onAction}){return <article className="faculty-card"><span className="faculty-badge">{project.status}</span><h3>{project.title}</h3><p>{project.description}</p><small><i className="bi bi-person-workspace"/> Lead: {project.lead}</small><small><i className="bi bi-people"/> Team: {project.members}</small><div className="faculty-tags">{project.tech.map(x=><span key={x}>{x}</span>)}</div><div className="faculty-actions"><button className="btn btn-sm btn-success" onClick={()=>onAction('approved',project.title)}>Approve</button><button className="btn btn-sm btn-outline-danger" onClick={()=>onAction('rejected',project.title)}>Reject</button><button className="btn btn-sm btn-outline-primary" onClick={()=>onAction('changes requested for',project.title)}>Request Changes</button></div></article>};export default ProjectReviewCard;

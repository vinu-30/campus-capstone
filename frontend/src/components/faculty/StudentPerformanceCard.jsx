// Table row for a student performance record and local feedback action.
function StudentPerformanceCard({student,onFeedback}){return <tr><td><strong>{student.name}</strong><small>{student.department}</small></td><td>{student.skills}</td><td>{student.role}</td><td><div className="progress"><div className="progress-bar" style={{width:`${student.completion}%`}}/></div>{student.completion}%</td><td>{student.comments}</td><td><button className="btn btn-sm btn-outline-primary" onClick={()=>onFeedback(student.name)}>Add Feedback</button></td></tr>};export default StudentPerformanceCard;

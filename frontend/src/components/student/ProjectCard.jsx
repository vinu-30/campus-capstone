// Shows a project opportunity and its required skills.
function ProjectCard({ project, onApply }) { return <article className="student-project-card"><div className="project-card-top"><span className="project-category">{project.category}</span><span><i className="bi bi-people" /> {project.membersNeeded} needed</span></div><h3>{project.title}</h3><p>{project.description}</p><div className="required-skills">{project.skills.map((skill) => <span key={skill}>{skill}</span>)}</div><button className="btn apply-button" onClick={() => onApply(project.title)}><i className="bi bi-send" /> Apply to Project</button></article>; }
export default ProjectCard;

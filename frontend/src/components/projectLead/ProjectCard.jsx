// Reusable project card used in the lead dashboard and project management page.
function ProjectCard({ project, onDelete }) { return <article className="lead-project-card"><div className="d-flex justify-content-between"><span className="lead-status">{project.status}</span><i className="bi bi-folder-fill text-primary" /></div><h3>{project.title}</h3><p>{project.description}</p><div className="lead-tags">{project.skills.map((skill)=><span key={skill}>{skill}</span>)}</div><small><i className="bi bi-people" /> {project.members} team members</small><div className="lead-actions"><button className="btn btn-sm btn-outline-primary">View</button><button className="btn btn-sm btn-outline-secondary">Edit</button>{onDelete&&<button className="btn btn-sm btn-outline-danger" onClick={()=>onDelete(project.title)}>Delete</button>}</div></article>; }
export default ProjectCard;

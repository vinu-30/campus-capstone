// Displays one applicant with action buttons for a project lead.
function ApplicationCard({ application, onAction }) { return <tr><td><strong>{application.studentName}</strong><small>{application.email}</small></td><td>{application.department}</td><td><strong>{application.projectTitle}</strong><div className="lead-tags">{application.skills.map((skill)=><span key={skill}>{skill}</span>)}</div></td><td>{application.experience}</td><td>{application.appliedAt}</td><td><span className={`application-status status-${application.status.toLowerCase()}`}>{application.status}</span></td><td className="d-flex gap-1 flex-wrap"><button className="btn btn-sm btn-outline-primary" onClick={()=>onAction(application.id,'Shortlisted')}>Shortlist</button><button className="btn btn-sm btn-success" onClick={()=>onAction(application.id,'Accepted')}>Accept</button><button className="btn btn-sm btn-outline-danger" onClick={()=>onAction(application.id,'Rejected')}>Reject</button></td></tr>; }
export default ApplicationCard;

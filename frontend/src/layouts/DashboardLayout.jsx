// Shared dashboard layout that manages mobile sidebar visibility.
import AppLayout from './AppLayout';
function DashboardLayout({ children }) { return <AppLayout>{children}</AppLayout>; }
export default DashboardLayout;
